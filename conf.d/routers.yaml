http:
  routers:
    DefaultHTTPSRedirect:
      entrypoints: ["http"]
      rule: "PathPrefix(`/`)"
      middlewares:
        - redirect
      service: "api@internal"
      priority: 1
      tls: {}